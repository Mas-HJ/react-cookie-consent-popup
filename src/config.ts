/** Prefix used for DOM element IDs created by this library */
export const ELEMENT_ID_PREFIX = 'rcc-popup';

/** Key used for persisting consent data in localStorage */
export const STORAGE_KEY = 'rcc-popup-consent';
